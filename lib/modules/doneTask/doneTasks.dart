import 'package:flutter/material.dart';

class doneTaskScreens extends StatelessWidget {
  const doneTaskScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'doneTasks',
      style: TextStyle(
        fontSize: 32,
        fontWeight: FontWeight.bold
      ),
    );
  }
}